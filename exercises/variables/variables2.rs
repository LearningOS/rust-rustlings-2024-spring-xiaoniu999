// variables2.rs
//
// Execute `rustlings hint variables2` or use the `hint` watch subcommand for a
// hint.

//rust声明变量可以暂时不初始化，可以延迟初始化，但声明变量必须给定值
fn main() {
    let x= 10;
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
